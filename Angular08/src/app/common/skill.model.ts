export class Skill{
    public name:string;
    public logo:string;
    public site:string;

    constructor(param_name:string, param_logo:string, param_site:string){

        this.name=param_name;
        this.logo=param_logo;
        this.site=param_site;
    }
}